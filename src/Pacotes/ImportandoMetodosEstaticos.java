package Pacotes;

import static java.lang.Math.*;
import static java.lang.Math.pow;
import static java.lang.Math.PI;

// Importar o metodo do java.lang como static dá a possibilidade de usar ele diretamente.

public class ImportandoMetodosEstaticos {

    public static void main(String[] args) {
        double n = Math.pow(3, 2);
        double n2 = pow(5,4);
        double pi = PI;
        double x = abs(-10);
    }
}
